dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: develop
    commands:
      - aws s3 sync /home/ubuntu/CircleCITest s3://circlecitest --delete
test:
  override:
    - echo "test"